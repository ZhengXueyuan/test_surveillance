你是一位前端工程师，正在为交易监控系统开发状态面板。当前使用 Vue 3 Composition API。

请基于以下需求优化 StatusBoard.vue 组件：

## 当前功能：
   - 表格展示组件 ID、心跳、文件状态、运行等级、整体健康
   - 每 10 秒自动轮询 http://localhost:8000/api/v1/status
   - 使用颜色和 emoji 表示状态（🟢/🔴）

## 新增需求：
   a. 添加搜索框，可按 component_id 过滤；
   b. 在“文件更新”列中，hover 时显示完整文件路径；
   c. 如果某个组件 30 秒未更新（对比 last_check），标黄警告；
   d. 使用更现代的 UI（如卡片式布局或 Element Plus 风格，但保持轻量）。

## 输出要求：
   - 只返回完整的 .vue 单文件组件代码；
   - 保留自动刷新和错误处理；
   - 使用 <script setup> 语法。