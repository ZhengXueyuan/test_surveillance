目前的展示只有文件缺失的场景，请增加文件存在的场景

## 场景描述
- 组件 ID：`market_data_feeder`
- 输入文件：`./data/market_data.csv`
- 预期更新频率：每 5 分钟（cron 表达式：`*/5 * * * *`）
- 要求：
  1. 脚本启动时自动创建 `./data/market_data.csv`（如果不存在）
  2. 每 30 秒向 FastAPI 后端（http://localhost:8000）发送一次心跳，包含 declared_level=2
  3. **每 4 分钟**（略早于 5 分钟周期）更新一次 `market_data.csv` 的内容和 mtime，模拟真实数据写入
  4. 使用 logging 记录关键事件（如“文件已更新”、“心跳已发送”）

## 技术要求
- 使用标准库：`os`, `time`, `json`, `requests`, `logging`
- 不要使用 APScheduler 或复杂依赖
- 脚本应可独立运行：`python sim/market_data_sim.py`
- 文件路径通过常量定义，便于修改

## 输出要求
- 只输出完整的 Python 脚本代码；
- 包含必要的注释；
- 代码需健壮（处理目录不存在、网络失败等）；
- 在脚本开头添加 usage 示例。