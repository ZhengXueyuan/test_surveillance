# 模拟交易系统组件状态监控系统设计

## 1. 三种监控维度的数据模型

### 1.1 组件运行状态 (Component Runtime Status)

```typescript
interface ComponentRuntimeStatus {
  // 唯一标识
  component_id: string;           // 组件ID，如 "trade_engine"
  
  // 进程存在性
  process_exists: boolean;        // 进程是否存活
  pid: number | null;             // 进程ID
  
  // 心跳信息
  last_heartbeat_at: string;      // ISO 8601 格式时间戳
  heartbeat_interval_sec: number; // 预期心跳间隔（秒）
  
  // 状态计算
  status: "healthy" | "warning" | "critical" | "offline"; // 综合状态
  
  // 元数据
  reported_at: string;            // 上报时间
  version: string;                // 组件版本号
  host: string;                   // 所在主机
}
```

**状态计算逻辑：**

| 条件 | 状态 |
|------|------|
| `now - last_heartbeat < interval * 1.5` | healthy |
| `interval * 1.5 <= 差值 < interval * 3` | warning |
| `差值 >= interval * 3` 或 `!process_exists` | critical/offline |

---

### 1.2 文件更新状态 (File Update Status)

```typescript
interface FileMonitorStatus {
  component_id: string;           // 关联组件ID
  
  input_files: FileStatus[];      // 输入文件状态列表
  output_files: FileStatus[];     // 输出文件状态列表
  
  overall_file_health: boolean;   // 所有文件是否健康
  checked_at: string;             // 检查时间戳
}

interface FileStatus {
  path: string;                   // 文件绝对路径
  type: "input" | "output";       // 文件类型
  
  // 计划配置
  expected_update_cron: string;   // 预期更新cron表达式
  
  // 实际状态
  last_modified: string;          // 最后修改时间
  file_size: number;              // 文件大小（字节）
  
  // 合规性判断
  is_compliant: boolean;          // 是否在预期周期内更新
  next_expected_update: string;   // 下次应更新时间
  grace_period_sec: number;       // 宽限期（秒）
  
  // 告警信息
  alert_message?: string;         // 不合规时的提示
}
```

---

### 1.3 组件运行等级 (Component Run Level)

```typescript
interface ComponentLevelStatus {
  component_id: string;           // 组件ID
  
  // 当前等级
  current_level: 1 | 2 | 3 | 4;   // 实际运行等级
  
  // 期望等级（基于时间段计算）
  expected_level: 1 | 2 | 3 | 4;  // 应达到的等级
  
  // 等级定义
  level_definition: {
    1: "started";       // 进程已启动
    2: "connected";     // 已连接外部系统
    3: "partial";       // 部分功能可用
    4: "fully_operational"; // 完全正常
  };
  
  // 合规性
  is_compliant: boolean;          // 当前等级 >= 期望等级
  
  // 当前应用的规则
  active_rule: {
    start_time: string;           // "HH:MM" 格式
    end_time: string;             // "HH:MM" 格式
    expected_level: number;
  };
  
  checked_at: string;             // 检查时间
}
```

---

## 2. YAML 计划文件模板

### 2.1 file_monitor_plan.yaml - 文件监控计划

```yaml
# 文件监控计划配置
# 描述每个组件依赖的输入文件和产生的输出文件及其预期更新周期

version: "1.0"
updated_at: "2026-02-23"

components:
  # ==================== 交易引擎 ====================
  - component_id: "trade_engine"
    description: "核心交易引擎，处理订单和成交"
    
    input_files:
      # 市场数据文件
      - path: "/data/market_data.csv"
        description: "实时市场数据"
        expected_update_cron: "0 */5 * * *"     # 每5分钟更新
        grace_period_sec: 60                    # 宽限期1分钟
        
      - path: "/data/reference_data.json"
        description: "参考数据（股票代码、价格限制等）"
        expected_update_cron: "0 7 * * 1-5"     # 工作日7:00更新
        grace_period_sec: 300

    output_files:
      - path: "/logs/trade_engine.log"
        description: "交易引擎运行日志"
        expected_update_cron: "*/1 * * * *"     # 每分钟应有输出
        grace_period_sec: 30
        
      - path: "/output/trades.csv"
        description: "成交记录"
        expected_update_cron: "*/5 * * * *"     # 每5分钟应有成交
        grace_period_sec: 60
        
      - path: "/output/positions.json"
        description: "持仓快照"
        expected_update_cron: "0 */10 * * *"    # 每10分钟更新
        grace_period_sec: 120

  # ==================== 风控检查器 ====================
  - component_id: "risk_checker"
    description: "实时风险检查模块"
    
    input_files:
      - path: "/config/risk_rules.json"
        description: "风控规则配置"
        expected_update_cron: "0 2 * * *"       # 每天凌晨2点更新
        grace_period_sec: 600
        
      - path: "/data/trade_engine/positions.json"
        description: "实时持仓数据（从trade_engine读取）"
        expected_update_cron: "0 */10 * * *"
        grace_period_sec: 120
        
      - path: "/data/market_prices.csv"
        description: "最新市场价格"
        expected_update_cron: "*/30 * * * * *"  # 每30秒更新
        grace_period_sec: 15

    output_files:
      - path: "/logs/risk_alerts.log"
        description: "风控告警日志"
        expected_update_cron: "*/1 * * * *"     # 持续输出
        grace_period_sec: 60
        
      - path: "/output/risk_report.json"
        description: "风险报告"
        expected_update_cron: "0 */15 * * *"    # 每15分钟生成
        grace_period_sec: 180

  # ==================== 市场数据接入 ====================
  - component_id: "market_data_feeder"
    description: "市场数据接入网关"
    
    input_files:
      - path: "/config/exchange_endpoints.json"
        description: "交易所接口配置"
        expected_update_cron: "0 1 * * 1"       # 每周一更新
        grace_period_sec: 3600

    output_files:
      - path: "/data/market_data.csv"
        description: "标准化市场数据（供trade_engine消费）"
        expected_update_cron: "*/30 * * * * *"  # 每30秒写入
        grace_period_sec: 15
        
      - path: "/logs/feeder_stats.log"
        description: "数据接入统计"
        expected_update_cron: "*/1 * * * *"
        grace_period_sec: 30

  # ==================== 策略执行器 ====================
  - component_id: "strategy_executor"
    description: "量化策略执行模块"
    
    input_files:
      - path: "/config/strategies/"
        description: "策略配置文件目录"
        expected_update_cron: "0 6 * * 1-5"     # 交易日6点加载
        grace_period_sec: 600
        is_directory: true
        
      - path: "/data/market_data.csv"
        description: "市场数据"
        expected_update_cron: "*/1 * * * *"
        grace_period_sec: 60

    output_files:
      - path: "/output/signals.json"
        description: "交易信号"
        expected_update_cron: "*/5 * * * *"
        grace_period_sec: 120
        
      - path: "/logs/strategy_perf.log"
        description: "策略性能日志"
        expected_update_cron: "0 */5 * * *"
        grace_period_sec: 180
```

---

### 2.2 level_schedule.yaml - 运行等级调度计划

```yaml
# 组件运行等级调度计划
# 定义每个组件在一天中不同时间段应达到的运行等级
# 
# 等级定义：
#   1 = started       - 进程已启动，基本初始化完成
#   2 = connected     - 已连接外部系统（数据库、消息队列、交易所等）
#   3 = partial       - 部分功能可用（如仅接收数据但不交易）
#   4 = fully_operational - 完全正常，所有功能可用

version: "1.0"
timezone: "Asia/Shanghai"          # 时区设置
market_calendar: "CN_STOCK"        # 市场日历（用于节假日判断）

components:
  # ==================== 交易引擎 ====================
  - component_id: "trade_engine"
    description: "核心交易引擎"
    
    rules:
      # 盘前准备
      - name: "pre_market"
        start_time: "08:30"
        end_time: "09:15"
        expected_level: 3           # 部分功能：接收数据、准备就绪
        description: "盘前准备，加载数据"
        
      # 连续竞价时段
      - name: "continuous_trading"
        start_time: "09:15"
        end_time: "11:30"
        expected_level: 4           # 完全正常：接受订单、执行交易
        description: "上午交易时段"
        
      # 午间休市
      - name: "lunch_break"
        start_time: "11:30"
        end_time: "13:00"
        expected_level: 3           # 部分功能：保持连接但不接受新订单
        description: "午间休市"
        
      # 下午交易时段
      - name: "afternoon_trading"
        start_time: "13:00"
        end_time: "15:00"
        expected_level: 4           # 完全正常
        description: "下午交易时段"
        
      # 收盘后
      - name: "post_market"
        start_time: "15:00"
        end_time: "15:30"
        expected_level: 2           # 连接状态：结算、生成报告
        description: "收盘后处理"
        
      # 夜间维护
      - name: "night_standby"
        start_time: "15:30"
        end_time: "08:30"
        expected_level: 1           # 仅保持进程存活
        description: "非交易时段"
        
    # 非交易日配置
    non_trading_day_level: 1        # 节假日仅保持level 1

  # ==================== 风控检查器 ====================
  - component_id: "risk_checker"
    description: "实时风险检查模块"
    
    rules:
      - name: "market_hours"
        start_time: "08:30"
        end_time: "15:30"
        expected_level: 4           # 交易时段完全监控
        
      - name: "off_hours"
        start_time: "15:30"
        end_time: "08:30"
        expected_level: 2           # 非交易时段保持连接
        
    non_trading_day_level: 1

  # ==================== 市场数据接入 ====================
  - component_id: "market_data_feeder"
    description: "市场数据接入网关"
    
    rules:
      - name: "data_session"
        start_time: "08:00"
        end_time: "17:00"
        expected_level: 4           # 全功能接收数据
        
      - name: "night_session"
        start_time: "17:00"
        end_time: "08:00"
        expected_level: 2           # 保持连接但不主动推送
        
    non_trading_day_level: 2        # 节假日保持连接

  # ==================== 策略执行器 ====================
  - component_id: "strategy_executor"
    description: "量化策略执行模块"
    
    rules:
      - name: "strategy_active"
        start_time: "09:00"
        end_time: "15:05"
        expected_level: 4           # 策略运行中
        
      - name: "strategy_idle"
        start_time: "15:05"
        end_time: "09:00"
        expected_level: 1           # 策略停止，仅保持进程
        
    non_trading_day_level: 1

  # ==================== 报表生成器 ====================
  - component_id: "report_generator"
    description: "日终报表生成服务"
    
    rules:
      - name: "daytime_standby"
        start_time: "09:00"
        end_time: "16:00"
        expected_level: 1           # 白天待机
        
      - name: "report_generation"
        start_time: "16:00"
        end_time: "18:00"
        expected_level: 4           # 生成报表
        
      - name: "night_standby"
        start_time: "18:00"
        end_time: "09:00"
        expected_level: 1           # 夜间待机
        
    non_trading_day_level: 1
```

---

## 3. 整体系统架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              模拟交易系统监控平台                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────┐    HTTP POST    ┌─────────────────────────────────┐  │
│   │  模拟交易组件 A   │ ──────────────> │                                 │  │
│   │  (trade_engine) │   /heartbeat    │      Backend Service            │  │
│   └─────────────────┘                 │      ┌─────────────────────┐    │  │
│                                        │      │   Heartbeat API     │    │  │
│   ┌─────────────────┐    HTTP POST    │      │   - POST /heartbeat │    │  │
│   │  模拟交易组件 B   │ ──────────────> │      │   - POST /status    │    │  │
│   │ (risk_checker)  │   /heartbeat    │      └─────────────────────┘    │  │
│   └─────────────────┘                 │                │                │  │
│                .                      │                v                │  │
│                .                      │      ┌─────────────────────┐    │  │
│   ┌─────────────────┐                 │      │   Status Query API  │    │  │
│   │  模拟交易组件 N   │ ──────────────> │      │   - GET  /status    │    │  │
│   │   (component_n) │                 │      │   - GET  /dashboard │    │  │
│   └─────────────────┘                 │      └─────────────────────┘    │  │
│                                        │                                 │  │
│                                        └─────────────────────────────────┘  │
│                                                      │                      │
│                                                      v                      │
│   ┌─────────────────┐    定时检查        ┌─────────────────────────────┐    │
│   │  定时任务服务     │ ─────────────────> │                             │    │
│   │ (Scheduler Cron)│   更新状态          │    Central State Store      │    │
│   │                 │                     │    ┌─────────────────────┐  │    │
│   │ • 文件更新检查    │ ─────────────────> │    │   Redis / Etcd      │  │    │
│   │ • 等级合规检查    │   更新状态          │    │   (Key-Value Store) │  │    │
│   │ • 状态聚合计算    │                     │    │                     │  │    │
│   └─────────────────┘                     │    │ Keys:               │  │    │
│        ▲                                  │    │ • runtime:{id}      │  │    │
│        │                                  │    │ • files:{id}        │  │    │
│        │ 读取计划文件                       │    │ • level:{id}        │  │    │
│        │                                  │    │ • health:{id}       │  │    │
│   ┌────┴─────────────┐                    │    └─────────────────────┘  │    │
│   │  YAML 配置文件    │                    │                             │    │
│   │ • file_monitor_  │                    │    单一真相源 (SSOT)         │    │
│   │   plan.yaml      │                    │    所有状态由此读取          │    │
│   │ • level_schedule │                    └─────────────────────────────┘    │
│   │   .yaml        │                                     │                  │
│   └──────────────────┘                                    │                  │
│                                                           │ HTTP GET         │
│                                                           v                  │
│   ┌──────────────────────────────────────────────────────────────────────┐  │
│   │                          Web Frontend                                │  │
│   │  ┌─────────────┐   ┌───────────────────┐   ┌─────────────────────┐  │  │
│   │  │  顶部导航栏   │   │    仪表盘视图      │   │     侧边栏菜单       │  │  │
│   │  │             │   │  ┌─────────────┐  │   │  • 组件总览          │  │  │
│   │  │ 实时监控平台  │   │  │ 状态概览卡片  │  │   │  • 文件监控          │  │  │
│   │  │ 用户/告警/设置│   │  ├─────────────┤  │   │  • 等级调度          │  │  │
│   │  └─────────────┘   │  │ 健康度图表    │  │   │  • 告警历史          │  │  │
│   │                    │  ├─────────────┤  │   │  • 系统设置          │  │  │
│   │  ┌─────────────┐   │  │ 组件列表      │  │   └─────────────────────┘  │  │
│   │  │  底部状态栏   │   │  └─────────────┘  │                            │  │
│   │  │ 系统状态摘要  │   └───────────────────┘                            │  │
│   │  └─────────────┘                                                      │  │
│   └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据流设计（数据面分离）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            数据流分离原则                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                           写入路径 (Write Path)                      │  │
│   │                                                                     │  │
│   │   交易组件 ──> HTTP POST ──> Heartbeat API ──> Redis (WRITE)        │  │
│   │                                                                     │  │
│   │   定时任务 ──> 检查文件/等级 ──> Status Updater ──> Redis (WRITE)   │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                    │                                        │
│                                    v                                        │
│                    ┌───────────────────────────────┐                        │
│                    │      Central State Store       │                        │
│                    │         (Redis)                │                        │
│                    │  ┌─────────────────────────┐   │                        │
│                    │  │  runtime:trade_engine   │   │                        │
│                    │  │  files:trade_engine      │   │                        │
│                    │  │  level:trade_engine      │   │                        │
│                    │  │  health:trade_engine     │   │                        │
│                    │  └─────────────────────────┘   │                        │
│                    │           ...                  │                        │
│                    └───────────────────────────────┘                        │
│                                    │                                        │
│                                    │ 唯一真相源                              │
│                                    v                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                           读取路径 (Read Path)                       │  │
│   │                                                                     │  │
│   │   Web Frontend ──> HTTP GET ──> Query API ──> Redis (READ ONLY)     │  │
│   │                                                                     │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│   ═══════════════════════════════════════════════════════════════════      │
│                                                                             │
│   数据面分离优势：                                                            │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │ 1. 组件只写 (Write-only)    │  组件不关心谁在消费数据，只管上报         │  │
│   │ 2. 前端只读 (Read-only)     │  前端不直接修改状态，通过API读取         │  │
│   │ 3. 单一真相源 (SSOT)        │  Redis是所有状态的唯一来源               │  │
│   │ 4. 自动容错                  │  读写隔离后，组件掉线不会污染前端显示     │  │
│   │ 5. 可扩展性                  │  可独立扩展读写端，互不影响              │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 核心模块职责

| 模块 | 职责 | 数据流向 |
|------|------|----------|
| **交易组件** | 执行业务逻辑，定时上报心跳 | 只写 → 中央存储 |
| **Heartbeat API** | 接收组件上报，写入Redis | 只写 → 中央存储 |
| **定时任务服务** | 检查文件更新、等级合规性，更新状态 | 读取配置 → 写入中央存储 |
| **Query API** | 提供状态查询接口 | 读取中央存储 → 返回前端 |
| **Web Frontend** | 可视化展示监控数据 | 只读 ← 中央存储 |

---

## 4. 网页结构

### 4.1 整体布局

基于参考图 `html_draft.png` 的设计，系统采用经典的**左侧边栏 + 顶部导航 + 主内容区 + 底部状态栏**布局：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  顶部导航栏 (Header)                                                          │
│  ┌────┬────────────────────────────────────────────────┬─────────────────┐  │
│  │Logo│  监控平台 / Component Status Monitor            │👤用户  🔔告警  ⚙️设置│  │
│  └────┴────────────────────────────────────────────────┴─────────────────┘  │
├──────────┬──────────────────────────────────────────────────────────────────┤
│          │  主内容区 (Main Content)                                          │
│          │  ┌────────────────────────────────────────────────────────────┐ │
│  侧边栏   │  │  过滤器栏 (Filter Bar)                                       │ │
│  (Sidebar)│  │  [全部状态 ▼] [所有组件 ▼] [最近1小时 ▼] [🔍 搜索...]         │ │
│          │  └────────────────────────────────────────────────────────────┘ │
│  ┌────┐  │  ┌────────────────────────────────────────────────────────────┐ │
│  │📊总览│  │                                                             │ │
│  ├────┤  │  │  状态概览卡片区 (Status Cards)                               │ │
│  │📁文件│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │ │
│  ├────┤  │  │ 🟢 正常    │ │ 🟡 警告    │ │ 🔴 严重    │ │ ⚫ 离线    │        │ │
│  │⚡等级│  │  │   12     │ │   3      │ │   1      │ │   2      │        │ │
│  ├────┤  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │ │
│  │🔔告警│  │                                                             │ │
│  ├────┤  │  ┌────────────────────────────────────────────────────────┐  │ │
│  │📈历史│  │  │  监控图表区 (Charts)                                    │  │ │
│  ├────┤  │  │  ┌────────────────┐  ┌────────────────┐              │  │ │
│  │⚙️设置│  │  │  │  健康度趋势     │  │  心跳延迟分布   │              │  │ │
│  └────┘  │  │  │   [折线图]      │  │   [柱状图]      │              │  │ │
│          │  │  └────────────────┘  └────────────────┘              │  │ │
│          │  │  ┌────────────────────────────────────────────────┐  │  │ │
│          │  │  │  文件更新状态热力图                              │  │  │ │
│          │  │  └────────────────────────────────────────────────┘  │  │ │
│          │  └────────────────────────────────────────────────────────┘  │ │
│          │  ┌────────────────────────────────────────────────────────┐  │ │
│          │  │  组件详细列表 (Component Detail Table)                  │  │ │
│          │  │  ┌─────────┬────────┬────────┬────────┬────────┬─────┐ │  │ │
│          │  │  │组件名称 │ 进程   │ 心跳   │ 当前等级│ 期望等级│ 状态 │ │  │ │
│          │  │  ├─────────┼────────┼────────┼────────┼────────┼─────┤ │  │ │
│          │  │  │trade_eng│  ✅    │  2s前  │   4    │   4    │ 🟢   │ │  │ │
│          │  │  │risk_chk │  ✅    │  5s前  │   4    │   4    │ 🟢   │ │  │ │
│          │  │  │data_fd  │  ✅    │  1m前  │   2    │   4    │ 🟡   │ │  │ │
│          │  │  │strat_exe│  ❌    │  5m前  │   -    │   4    │ 🔴   │ │  │ │
│          │  │  └─────────┴────────┴────────┴────────┴────────┴─────┘ │  │ │
│          │  └────────────────────────────────────────────────────────┘  │ │
│          │                                                               │ │
├──────────┴───────────────────────────────────────────────────────────────┤
│  底部状态栏 (Footer Status Bar)                                             │
│  ┌────────────────────┬────────────────────┬───────────────────────────┐  │
│  │  系统: 正常运行     │  最后更新: 10:06:28 │  WebSocket: 已连接 ■       │  │
│  └────────────────────┴────────────────────┴───────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 各区域详细说明

#### 4.2.1 顶部导航栏 (Header)
- **Logo区**：系统名称和Logo
- **导航菜单**：快速切换不同监控视图
- **右侧功能区**：
  - 用户头像/下拉菜单
  - 告警铃铛（带未读数量角标）
  - 设置按钮

#### 4.2.2 侧边栏菜单 (Sidebar)
- **总览**：系统整体健康度仪表盘
- **组件监控**：各组件详细状态列表
- **文件监控**：输入/输出文件更新状态
- **等级调度**：运行等级合规性视图
- **告警历史**：历史告警记录查询
- **系统设置**：通知配置、阈值设置等

#### 4.2.3 主内容区 (Main Content)

**状态概览卡片区**：
- 显示四种状态的组件数量统计
- 支持点击筛选

**监控图表区**：
- 系统健康度趋势图（24小时）
- 心跳延迟分布图
- 文件更新合规率热力图

**组件详细列表**：
- 表格展示所有组件
- 列：组件名、进程状态、最后心跳、当前等级、期望等级、综合状态
- 支持排序、筛选、分页
- 点击行可展开查看详情

#### 4.2.4 底部状态栏 (Footer Status Bar)
- 系统整体运行状态
- 数据最后更新时间
- WebSocket连接状态指示

### 4.3 响应式布局考虑

| 断点 | 布局调整 |
|------|----------|
| `>= 1440px` | 侧边栏展开，图表并排显示 |
| `1024px - 1439px` | 侧边栏可折叠，图表堆叠 |
| `< 1024px` | 侧边栏变为抽屉式，简化图表 |

---

## 5. 数据结构关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          监控数据关系图                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                           ┌─────────────────────┐                          │
│                           │   Component Def     │                          │
│                           │   (YAML配置定义)     │                          │
│                           └──────────┬──────────┘                          │
│                                      │                                      │
│           ┌──────────────────────────┼──────────────────────────┐          │
│           │                          │                          │          │
│           v                          v                          v          │
│  ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐    │
│  │ Runtime Status  │      │ File Monitor    │      │ Level Schedule  │    │
│  │ (运行状态)       │      │ (文件监控)       │      │ (等级调度)       │    │
│  │                 │      │                 │      │                 │    │
│  │ • process_exists│      │ • input_files[] │      │ • expected_level│    │
│  │ • last_heartbeat│      │ • output_files[]│      │ • time_rules[]  │    │
│  │ • status        │      │ • is_compliant  │      │ • is_compliant  │    │
│  └────────┬────────┘      └────────┬────────┘      └────────┬────────┘    │
│           │                        │                        │              │
│           └────────────────────────┼────────────────────────┘              │
│                                    │                                      │
│                                    v                                      │
│                    ┌───────────────────────────────┐                      │
│                    │     Aggregated Health         │                      │
│                    │     (聚合健康度)               │                      │
│                    │                               │                      │
│                    │  overall_status = f(runtime,  │                      │
│                    │                   files,      │                      │
│                    │                   level)      │                      │
│                    └───────────────┬───────────────┘                      │
│                                    │                                      │
│                                    v                                      │
│                    ┌───────────────────────────────┐                      │
│                    │      Dashboard View           │                      │
│                    │      (前端展示)                │                      │
│                    └───────────────────────────────┘                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 关键技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 中央存储 | Redis | 高性能、支持TTL、Pub/Sub适合实时推送 |
| 配置格式 | YAML | 人类可读、支持注释、适合版本控制 |
| 通信协议 | HTTP REST + WebSocket | HTTP用于状态上报，WebSocket用于前端实时更新 |
| 定时任务 | Cron表达式 | 行业标准、表达能力强、易于配置 |
| 等级设计 | 1-4级 | 粒度适中，覆盖启动→连接→部分→完全 |

---

*文档生成时间: 2026-02-23*
