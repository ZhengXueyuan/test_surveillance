你是一位前端工程师，正在为交易监控系统开发“趋势图”功能。请基于以下需求，在 'StatusBoard.vue' 中添加趋势图：

## 需求描述
- 在主内容区添加一个折线图，展示某个组件健康状态的变化趋势
- 图表应支持动态更新（每 10 秒自动刷新）
- 使用 Chart.js 或 ECharts 来绘制图表（建议用 CDN 方式加载）
- 支持选择不同组件查看其历史数据（如 dropdown 切换）

## 技术要求
- 使用 Vue 3 Composition API
- 引入 Chart.js/ECharts 时使用 CDN
- 返回完整的 .vue 单文件组件代码片段，仅包含新增部分

## 输出要求
- 更新到文件中